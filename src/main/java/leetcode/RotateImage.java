package leetcode;

/**
 * Created by fuyul on 3/31/2018.
 */
public class RotateImage {
    public void rotate(int[][] matrix) {
        int layer = matrix.length / 2;
        for (int i = 0; i < matrix.length/2; i++) {
            int tmp = matrix[i][i];
            for (int j = 0; j < matrix.length - 1; j++) {
             //   tmp =
            }
        }
    }
}
